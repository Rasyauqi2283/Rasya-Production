# Server
PORT=8080
ENV=development

# PostgreSQL (optional). Jika diisi, semua data donasi/layanan/porto/order/taper disimpan di DB (real-time persistent).
# Format: postgres://user:password@host:port/dbname?sslmode=disable
# Contoh lokal: postgres://postgres:password@localhost:5432/raspro?sslmode=disable
# DATABASE_URL=

# Donasi: rekening untuk transfer (ditampilkan setelah user submit form)
BANK_NAME=BCA
BANK_NUMBER=1234567890
BANK_ACCOUNT=Rasya

# Midtrans (donasi GoPay Merchant). Daftar di https://dashboard.sandbox.midtrans.com (sandbox) atau dashboard.midtrans.com (production).
# Server Key dan Client Key ada di Settings → Access keys. Isi untuk aktifkan bayar donasi via GoPay.
MIDTRANS_SERVER_KEY=
MIDTRANS_CLIENT_KEY=
# MIDTRANS_IS_PRODUCTION=false
# Webhook: di dashboard Midtrans → Settings → Configuration → Notification URL isi: https://your-api-domain.com/api/donate/webhook

# Batas donasi (IDR) agar masuk "prioritas inbox" (default 50000)
# DONATE_HIGHLIGHT_IDR=50000

# Admin: hanya email Google ini yang bisa masuk ke laman admin (login dengan Google).
ADMIN_ALLOWED_EMAIL=rasyauqi2283@gmail.com

# JWT: secret untuk tanda-tangan token admin (pakai string acak yang kuat).
JWT_SECRET=your-jwt-secret-at-least-32-chars
