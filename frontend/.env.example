# ---- Development lokal: frontend + backend ----
# Frontend: cd rasya-production/frontend && npm run dev  → http://localhost:3000
# Backend:  cd rasya-production/backend/go && go run ./cmd/api  → http://localhost:8080
# (Backend butuh .env di backend/go: PORT=8080, DATABASE_URL=..., dll. Lihat backend/go/README.md)

# API backend (untuk fetch donasi, porto, admin, dll.). Lokal pakai localhost:8080.
NEXT_PUBLIC_API_URL=http://localhost:8080

# URL website frontend. Jika tidak diset, dipakai http://localhost:3000 (agar dev lokal jalan).
# Production: set ke domain Anda, mis. https://www.raspro.online atau https://raspro.co.id
# NEXT_PUBLIC_SITE_URL=https://www.raspro.online

# Maintenance: set "true" di production untuk menampilkan laman maintenance saja.
# Lokal biar false atau tidak diset agar bisa development.
# NEXT_PUBLIC_MAINTENANCE=false

# Nomor WhatsApp Bisnis (format: 62xxxxxxxxxx tanpa + atau 0 depan). Dipakai untuk link wa.me dan CTA kontak.
NEXT_PUBLIC_WHATSAPP_NUMBER=6281234567890

# Midtrans: true = production Snap, false/kosong = sandbox (untuk tes donasi GoPay).
# NEXT_PUBLIC_MIDTRANS_IS_PRODUCTION=false

# QRIS donasi: URL gambar QRIS Anda (atau kosongkan = pakai file public/qris.png).
# Contoh: /qris.png (file di frontend/public/qris.png) atau URL penuh ke gambar.
# NEXT_PUBLIC_QRIS_IMAGE_URL=/qris.png

# Google OAuth: Client ID untuk "Masuk dengan Google" di laman admin.
# Buat di Google Cloud Console: APIs & Services → Credentials → Create OAuth 2.0 Client ID (Web application).
# Authorized JavaScript origins: http://localhost:3000 dan URL production (mis. https://rasyaproduction.up.railway.app).
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
